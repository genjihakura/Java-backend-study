server:
  port: 6969
spring:
  profiles:
    include: native
  cloud:
    config:
      server:
        native:
#          searchLocations: ${CONFIG_LOCATION}
          searchLocations: file:config-server/configs
